<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.first.myapplicationfirst">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/storming"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/storming_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationFirst">
        <activity
            android:name=".MainBn"
            android:exported="true"
            android:label="@string/title_activity_main_bn"
            android:theme="@style/Theme.MyApplicationFirst.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Signup"
            android:exported="true"
            android:parentActivityName=".LoginActivity">


        </activity>
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/NoActionTheme">
            <!-- <intent-filter>
                 <action android:name="android.intent.action.MAIN" />

                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>-->
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/NoActionTheme"></activity>
    </application>

</manifest>